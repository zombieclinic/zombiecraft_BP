import {world} from "@minecraft/server"
import { ActionFormData } from "@minecraft/server-ui"

import class Conjuration {
    onPlayerInteract (arg) {
        player = player.arg

        teleportation(player);
    }
}



function teleportation(player) {
    new ActionFormData()
    .title ("WarpAlter")
    .Body ()
}